# ===========================================
# ?? ??? ?? ?? (apps/backend/.env)
# ?? ??? .env.production.template ??
# ===========================================

# ?????? ??? (??? ?? ? ??)
STORE_CORS=http://localhost:8000

# Admin / ?? CORS
ADMIN_CORS=http://localhost:5173,http://localhost:9000,http://127.0.0.1:5173,http://127.0.0.1:9000
AUTH_CORS=http://localhost:5173,http://localhost:9000,http://127.0.0.1:5173,http://127.0.0.1:9000

# ?? ??? ??? (?? ? ??? ??)
JWT_SECRET=dev-jwt-secret-change-in-production
COOKIE_SECRET=dev-cookie-secret-change-in-production

# PostgreSQL
# ??: postgres://postgres:postgres@localhost:5432/group_buying
# Supabase: Project Settings ? Database ? Connection string (URI)
#   - ??????: Direct connection (?? 5432)
#   - ? ??: Transaction pooler (?? 6543) ??
DATABASE_URL=postgres://postgres:postgres@localhost:5432/group_buying

# Supabase ? ?? DB ?? ? true (?? Postgres? ???? false)
# DATABASE_SSL=true

# Redis (????? ???? in-memory ??)
# REDIS_URL=redis://localhost:6379

# Worker mode: shared | server | worker
# MEDUSA_WORKER_MODE=shared

# ===========================================
# Flask AI/search engine sync (Hybrid API)
# ===========================================
# Flask? ?? ???? ??? ? ???? Medusa store base URL
# STORE_PUBLIC_URL=http://localhost:9000

# ===========================================
# ?????? (?? ?? ??)
# ===========================================
TOSS_TEST_MODE=true
TOSS_WEBHOOK_SECRET=
TOSS_SUPPORTED_COUNTRIES=kr

# ?????? API ?: TOSS_SECRET_KEY=
TOSS_CLIENT_KEY=

# ??? ???? ??? (??? ? COOKIE_SECRET ??)
BILLING_KEY_ENCRYPTION_SECRET=

# ???? ??? ?? ??? ??
PG_CAPTURE_MAX_RETRIES=3

# ===========================================
# Stripe (?? ?? ???? SetupIntent ??)
# ===========================================
STRIPE_TEST_MODE=true
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
# ??? ??(kr) ?? ?? ??. ?: us,gb,de,jp
STRIPE_SUPPORTED_COUNTRIES=
